# Basic Sentiment Analyzer

## Pre-reqs:
* C++ Compiler
* Make
* Have a folder named `words` with 2 files inside, a `positive.txt` file containing positive words delimited by a new line and a `negative.txt` containing negative words delimited by a new line.

## Usage:
* Run `make`
* Run `build/output`